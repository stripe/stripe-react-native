# Validates that the new architecture and bridgeless mode are enabled.
appId: ${APP_ID}
---
- launchApp:
    clearState: true
- assertVisible: 'New arch enabled: true'
- assertVisible: 'Bridgeless enabled: true'
